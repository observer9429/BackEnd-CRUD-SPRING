spring.application.name=basic

server.port=9091

# --- CONEXIÓN A POSTGRES LOCAL ---
spring.datasource.url=jdbc:postgresql://localhost:5432/security_basico
spring.datasource.username=postgres
spring.datasource.password=Diris.2027ws

spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2

# --- JPA / Hibernate  --- el show-sql es apra ver las consultas que se hacen, pero en la consola---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true 
spring.jpa.properties.hibernate.format_sql=true